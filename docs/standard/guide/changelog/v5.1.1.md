# v5.1.1 更新日志

> 发布日期：2026-09-11 · 标准版
>
> **推送状态：未推送**（backend 与 template/admin 两个仓库均停留在本地 main 分支）

本文档记录 v5.1.1 补丁版本的具体变更。跨版本升级前请先阅读[升级指南](./upgrade.md)。

---

## 后端更新（backend/）

### ✨ 新增 MCP 协议支持

全新的 MCP（Model Context Protocol）接入层，让 AI Agent 可通过标准协议调用系统能力。

- **MCP 核心框架**（`core/communication/mcp/`）
  - `ServerFactory` + `McpInstanceContainer`：基于 Koa 的独立 MCP HTTP Server 与 SSE 流式传输
  - `Authenticator` + `ApiKeyProvider` + `RedisSessionStore`：API Key 鉴权与 Redis 会话
  - `ToolManifest` + `Finder`：自动扫描 `plugin/*/app/mcp/` 下的工具类并构建清单，文件 mtime 驱动清单重建
  - `#[McpTool]` 注解 + `PsrRequestFactory` / `ResponseEmitter`：将 HTTP 请求桥接到 MCP 协议栈

- **内置 MCP 工具集**（20+ 工具，按域划分）

  | 域 | 工具 | 用途 |
  |----|------|------|
  | `ops/` | Ping / HealthCheck / ServerMonitor / SystemInfo / CacheStatus / CacheClear / QueueStatus | 运维诊断 |
  | `dev/` | CodeSearch / DbSchema / LogTail / RouteList | 开发辅助 |
  | `ci/` | DbQuery / JobTrigger | 数据库查询与任务触发 |
  | `catalog/` | DictCatalog / MenuCatalog / PluginList / ToolCatalog | 枚举查询 |
  | `system/` | CurrentUser / AdminAccountQuery / ConfigRead / SchedulerStatus | 系统信息 |
  | 内置 | AdminUserList / RoleList / MadongIdentityResolver | 管理员身份桥接 |

- **CLI 命令**（`app/command/mcp/`）
  - `php webman mcp:serve` — 启动独立 MCP HTTP Server
  - `php webman mcp:list [--rebuild]` — 列出已注册工具并可选重建清单
  - `php webman mcp:call <tool> [--params json]` — 本地调用单个工具（用于验证）

- **插件 MCP 工具规范**：业务插件在 `app/mcp/{domain}/` 下按功能域放置，使用 `WorkflowClient` 等 SDK 访问数据，所有工具为只读查询

### 🔧 后端修复与重构

- 新增工作流异常类型字段迁移 `2026_09_10_000001_add_abnormal_type_to_wf_process_instance.php`
- 修复 `SseHelper` 响应辅助类的内容输出问题
- `composer.json` 升级 phpunit 10.0 → 10.5，更新依赖锁
- `windows.php` 启动脚本适配
- `phpunit.xml` 测试配置扩展
- `.gitignore` 补充：排除 `packages/` 独立引擎包、临时诊断脚本（`.dbg/`、`HANDOVER.md`、`check-messages.php`、`insert-test-message.php`）和测试骨架占位（`tests/integration/`、`tests/saas/`），仅保留 `tests/Bootstrap.php`

### 📦 插件 download 导出

为 `codegen`、`logviewer`、`workflow` 三个插件新增 `backend/plugin/TransferCommand` 支持，可将插件从项目提取到 `download/{name}/` 独立仓库结构，包含后端代码、Admin 前端模板、配置与迁移。

### 📁 仓库组织变更

- `config/route.php` 新增 `require_once core/communication/mcp/route.php`，在 `Route::disableDefaultRoute()` 之前注册固定路径 `/mcp`
- `resource/data/menu/admin.php` 追加 MCP 相关菜单节点

### ⚠️ 未入库的 packages

`packages/` 目录下的 `workflow`（独立引擎）和 `workflow-laravel`（Laravel 适配层）**暂不入库**，已通过 `.gitignore` 排除，未来作为独立仓库管理。

---

## 前端更新（template/admin/）

### 🐛 插件路径统一修复

后端插件前端目录从 `plugins/` 复数形式统一为 `plugin/` 单数形式（与项目约定保持一致）。

涉及文件：
- `src/core/shared/helpers/generate-routes-backend.ts` — 全局替换 `plugins` → `plugin`（约 20 处）
- `src/router/access.ts` — 权限路由适配单复数路径
- `src/core/access/accessible.ts` — 可访问性检查适配

### ✨ 消息通知增强

- `src/views/content/message/notify/index.vue`：列表页重构（+189 行）
- `src/views/content/message/notify/components/message-drawer.vue`：抽屉组件适配
- `src/components/dialog/dialog.vue`：支持无 `dialogType` 模式的对话框
- i18n：`zh-CN` / `en-US` 通知文案同步更新

### 🔧 构建与样式

- `package.json` 版本 5.7.0 → 5.7.1
- `build/vite/config/application.ts` / `vite.config.ts`：构建配置调整
- `src/core/design/styles/design/css/transition.css`：过渡动画增强
- `src/preferences.ts`：偏好设置扩展

### 🆕 新增文件

- `src/utils/navigation.ts`：导航辅助工具函数（86 行）

---

## 升级提示

1. **后端**：`git pull` → `composer install` → `php webman phinx migrate`（新增工作流异常类型迁移）→ 重启 Webman 进程
2. **前端**：`git pull` → `pnpm install` → `pnpm build` → 部署产物
3. **MCP 启用**：确认 `config/mcp.php` 配置正确后，通过 `php webman mcp:serve` 启动独立 MCP HTTP Server，或在主进程中加载

---

## 提交历史（供参考）

### backend/

| Hash | Type | 说明 |
|------|------|------|
| `16a60fc` | chore | 补充 .gitignore 排除测试骨架占位目录 |
| `46b84bb` | fix | 修复 SSE Helper 响应并补充测试引导入口 |
| `6be1204` | chore | 更新 composer 依赖锁、phpunit 配置与 Windows 启动脚本 |
| `4515fbe` | feat | 集成 MCP 路由、配置、菜单与工作流异常类型迁移 |
| `0afc19d` | feat | 新增 MCP 工具集（ops/dev/ci/catalog/system）与内置用户角色工具 |
| `f277abe` | feat | 新增 MCP 核心框架（Server/Security/Discovery/CLI） |

### template/admin/

| Hash | Type | 说明 |
|------|------|------|
| `bd4d880` | feat | 新增导航工具函数 |
| `cf78ff2` | chore | 升级 Vben 版本、构建配置与过渡动画样式 |
| `16dcf9d` | feat | 增强消息通知页面与对话框组件适配 |
| `6803f1b` | fix | 修复插件路径 /plugins 改为单数 /plugin |

---

*本文档为本次更新的完整变更记录。两个仓库均处于本地 main 分支未推送状态，推送前请在各自仓库执行 `git push`。*
