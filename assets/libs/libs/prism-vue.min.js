/* Offline fallback: alias `vue` to `markup` so Vue code blocks highlight
   without fetching the (removed) PrismJS vue component from a CDN. */
(function () {
  if (window.Prism && Prism.languages && Prism.languages.markup) {
    Prism.languages.vue = Prism.languages.markup;
  }
})();
