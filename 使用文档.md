# 使用文档

本文档介绍 **Madong 文档中心（`docs`）** 本身的使用方式：门户导航、密钥门禁、阅读体验、本地预览、部署与新增手册。

文档站基于 [Docsify](https://docsify.js.org/) 构建，**直接读取 `.md` 源文件渲染，无需打包、无需构建**。

---

## 一、目录结构

```
docs/
├── index.html        # 门户首页（卡片索引，由 manuals.json 驱动）
├── manuals.json      # 手册注册表（单一事实来源）
├── 使用文档.md        # 本文档（源）
├── 使用文档.html      # 本文档的 Docsify 渲染页
├── assets/           # 站点级共享：libs/ css/ js/ img/
└── manuals/          # 各手册按 id 独立目录（互不影响）
    ├── _template/    # 脚手架：复制即用
    ├── standard/     # 标准版手册
    └── saas/         # 多租户版手册
```

- 每册手册是**独立 Docsify 实例**，删除 `manuals/<id>/` 即移除该手册及其图片，不影响其他册。
- 图片统一放 `manuals/<id>/assets/img/<板块>/`，正文用 `../assets/img/<板块>/x.png` 引用。

---

## 二、门户导航

门户首页 `docs/index.html` 以卡片展示全部手册：

- **搜索**：顶部输入框按「名称 / 描述 / 标签」实时筛选。
- **状态筛选**：全部 / 在用 / 建设中 / 已废弃（对应手册 `status` 字段）。
- **进入手册**：点击卡片「进入手册 →」跳转至对应手册的 Docsify 实例。

---

## 三、密钥门禁

手册支持**三种粒度**的访问控制，由 `assets/js/gate.js` 控制（SHA-256(salt + 输入) 比对，通过后记 `localStorage`）。**不再整站一刀切**——可整册共享密钥、排除/限定某些文档、或对单篇文档设置独立密钥。

1. **整册共享站点密钥（默认）**：手册 `index.html` 中 `access.default: 'protected'` 且 `keyId: 'default'`，与门户共用站点密钥，解锁一次全站通行。
2. **排除 / 限定某些文档**：在 `access.rules` 写匹配规则，`access: 'public'` 即免密公开，其余仍走密钥模式。
3. **单篇文档独立密钥**：`rules` 中某条 `access: 'protected'` 且 `keyId` 指向 `access.policies` 中注册的独立策略。

对象式配置（写到手册 `index.html` 的 `window.__MANUAL__.access`）：

```js
access: {
  default: 'protected',          // 本手册默认模式：'public' 全册免密 / 'protected' 走密钥
  keyId: 'default',              // 默认密钥策略 id：'default' = 站点级共享密钥
  rules: [                       // 文档级规则，按顺序命中首个；match 支持 * 单层、 ** 任意深度
    { match: 'guide/intro/**',     access: 'public' },                  // 排除：介绍整节免密
    { match: 'guide/internal/**',  access: 'protected', keyId: 'vip' }   // 局部独立密钥：内部资料
  ],
  policies: {                    // 本手册独立密钥策略；未列出则回落站点默认
    vip: { salt: 'vip-salt', hash: '<摘要>', title: 'VIP 内部资料', tip: '请输入内部资料密钥' }
  }
}
```

- `match` 路径**相对手册根目录**，已自动去掉 `.md`，例如 `guide/intro/index.md` 写作 `guide/intro/**` 或 `guide/intro/index`。
- 多个策略的 `unlock` 各自独立记 `localStorage`，互不干扰：切换手册 / 打开独立密钥文档时会按对应策略重新要密钥。
- 计算摘要：浏览器控制台 `await DocsifyGate.compute('新密钥')`（默认盐），自定义盐用 `await DocsifyGate.compute('新密钥', '你的salt')`；或本地 PowerShell：
  ```powershell
  $s='madong-docs你的新密钥'; (Get-FileHash -Algorithm SHA256 -InputStream ([System.IO.MemoryStream]::new([System.Text.Encoding]::UTF8.GetBytes($s)))).Hash
  ```
- 密钥仅前端弱校验，**请勿在公开站点放置敏感内容**。

---

## 四、阅读体验

- **左侧菜单**：垂直滚动条已隐藏但保留滚动（鼠标滚轮 / 触控板 / 拖拽滚动均可用）；带子级的菜单可点击标题折叠 / 展开。
- **右侧目录（TOC）**：自动提取正文 `h2/h3` 生成树形目录，悬浮定位、滚动高亮。
- **搜索**：手册右上角搜索框，仅索引本册已加载页面。
- **代码复制 / 返回顶部 / 图片灯箱**：正文代码块右上角「复制」，长文右下角「↑」返回顶部，点击图片放大。

---

## 五、本地预览

Docsify 在浏览器中通过 `fetch` 读取 `.md`，因此**不能直接双击 `index.html`（`file://`）打开**——浏览器会拦截本地文件读取，表现为空白或 404。需在 `docs/` 目录下起一个本地静态服务：

```bash
npx docsify-cli serve docs
# 浏览器打开 http://localhost:3000/
```

此方案为**完全离线**（依赖已本地化在 `assets/libs/`），断网也能正常渲染。

---

## 六、部署

部署即「把 `docs/` 整目录放到任意静态服务器根下」，不需要构建步骤，入口文件为 `index.html`。

### 6.1 小皮 / phpStudy

「网站」中添加站点，将**网站根目录指向 `docs/`**，并将**入口/默认文档设为 `index.html`** 即可。

### 6.2 Nginx

```nginx
server {
    listen 80;
    server_name docs.example.com;
    root /path/to/docs;        # 指向 docs 目录
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;   # Docsify hash 路由回落
    }
}
```

如需放到子路径（如 `https://host/docs/`），把 `root` 设为 `docs/` 的上一级并 `location /docs/ { ... }` 即可，无需改代码。

### 6.3 对象存储 / GitHub Pages

将 `docs/` 整目录上传，开启静态网站托管，入口文件设为 `index.html`。

---

## 七、新增手册

1. 复制脚手架：`cp -r docs/manuals/_template docs/manuals/<id>`。
2. 改 `docs/manuals/<id>/index.html` 的 `window.__MANUAL__`：`id` 与目录名一致、`name` 显示名、`access` 模式（见第三节）。
3. 填 `docs/manuals/<id>/manual.json`（`name / type(app|plugin) / desc / version / status / tags`）。
4. **在 `docs/manuals.json` 的 `manuals` 数组注册一行**（漏注册则不显示）。
5. 在 `_sidebar.md` / `_navbar.md` 登记本手册导航，正文放到 `guide/`。

> 多手册隔离：每册独立实例，删除 `manuals/<id>/` 即移除该手册及其图片，不影响其他册。

更详细的「不动既有内容追加内容」规范，详见 `AI-GUIDE.md`（面向 AI / 贡献者）。
