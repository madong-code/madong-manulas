# Naive UI · 快速上手（骨架）

> 预留。步骤同 [`../admin-ele/getting-started.md`](../admin-ele/getting-started.md)，组件名改为 Naive（`Input` → `n-input`），并完成 `adapter/component` / `adapter/form` 的 Naive 注册。

**待填充：** 安装 naive-ui、切换 UI、CRUD 最小路径。
