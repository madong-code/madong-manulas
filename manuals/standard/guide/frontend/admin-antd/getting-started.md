# Ant Design Vue · 快速上手（骨架）

> 预留。落地步骤与 [`../admin-ele/getting-started.md`](../admin-ele/getting-started.md) 一致，仅组件名由 Element Plus 改为 AntD（`Input` → `a-input` 等），且 `adapter/component/index.ts` 与 `adapter/form.ts` 已完成 AntD 注册。

**待填充内容：**
- 安装 ant-design-vue 依赖
- 切换 `.env` UI 标识
- 一个 CRUD 页面的最小路径（复用 `views/**` + `api/**` + `CrudSchema`）
