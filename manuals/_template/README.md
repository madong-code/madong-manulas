# <手册名>

<一句话描述本手册的适用范围与读者对象。>

## 资源与图片约定

- **静态图片**：放在本手册 `assets/img/<板块>/` 目录，按板块分子目录。
- **文档引用**（在 `guide/` 下的 `.md` 中用相对路径）：
  ```md
  ![说明](../assets/img/<板块>/example.png)
  ```
- 站点运行时（Docsify）由 `../../assets/libs/` 共享，请勿在手册内放置。
- 本手册为独立实例，正文与 `_sidebar.md` 的相对关系不受其他手册影响。
